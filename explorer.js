  var game = (Math.random(566)*909.90)
  console.info (game)

  function mathGame (min,max){
      return (Math.random(min)*max)
  }
  console.info(mathGame(9090,88990))

  var game = (Math.random(566)*909.90)
  console.info (game)

  function mathGame (min,max){
      return (Math.random(min)*max)
  }
 console.info(mathGame(9090,88990))

 //